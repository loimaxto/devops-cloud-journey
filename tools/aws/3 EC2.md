## 1 Create EC2 instance
## 1 

### 2 Network setting
- Auto assign publick IP : alllow ssh

### 3 ssh config

```bash
mv file.pem  ~/.ssh/
chmod 400 .ssh/file.pem
ssh -i ~/.ssh/file.pem use@10.0.0.0
```
